import * as React from 'react';
import { Url } from 'url';
import { ContributeType } from '../../../../types/contribute';

interface Props {
    src: string | undefined;
}

export const RepeatClipPlayButton: React.FunctionComponent<Props> = (props) => {
    return <div>Hej this works</div>
}